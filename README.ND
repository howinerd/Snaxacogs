# Snaxa Vending

Price intelligence platform for vending machine operators.

## Setup

1. Create a virtual environment: `python -m venv venv`
2. Activate the environment: `venv\Scripts\activate`
3. Install dependencies: `pip install -r requirements.txt`
4. Set up environment variables in `.env`
5. Initialize the database: `flask db upgrade`
6. Run the application: `run.bat`

## Development

- Start all components: `start_all.bat`
- Run only the web app: `run.bat`
- Run only the worker: `start_worker.bat`